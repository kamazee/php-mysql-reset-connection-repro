server {
    listen 8080;
    root /app;

    location = /healthz {
        return 204;
    }

    location = /repro-fpm.php {
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /app/repro-fpm.php;
        fastcgi_pass 127.0.0.1:9000;
    }
}
